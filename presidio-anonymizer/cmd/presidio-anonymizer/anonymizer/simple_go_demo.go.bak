package main

import (
	"fmt"
	"time"
)



func main() {
	dateLayout := "02th-of-January"
	t, err := time.Parse(dateLayout, "30th-of-July")
	if err==nil {
		fmt.Println(t.Format(dateLayout))		
	}else{
		fmt.Println(err)
	}

}
